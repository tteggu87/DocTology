# Ralph Progress Log
Started: 2026-08-25T00:00:00+09:00

## Codebase Patterns
- `AGENTS.md` and bootstrap-generated scripts own workspace-specific procedure; reusable skills should orchestrate, not duplicate that contract.
- Canonical completion evidence already lives in `state/wiki_runs/` and `state/wiki_batches/`; do not add a second workflow ledger.
- `.codex/`, `.omo/`, and `docs/reviews/` contain pre-existing user artifacts. Stage only the explicit Ralph files and story-owned source/test changes.
---
## 2026-08-25T02:15:13+09:00 - US-001
- Refocused the repository-local `llm-wiki-ontology-ingest` skill as a capability-aware operator with explicit ownership boundaries for bootstrap/runtime, orchestration, and ontology truth conventions.
- Added contract-driven selection for `llm-first-ontology`, `wiki-plus-ontology`, and `wiki-only`, with command existence checks and no unconditional `llm_full_ingest.py` recommendation.
- Preserved the closed-pipeline and semantic no-fallback contracts and added a focused regression test.
- Files changed: `.agents/skills/llm-wiki-ontology-ingest/SKILL.md`, `tests/test_llm_wiki_pipeline_contract.py`, `.codex/ralph/prd.json`, `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest tests/test_llm_wiki_pipeline_contract.py -q` -> pass: 15 tests and 3 subtests.
  - `python3 -m pytest -q` -> pass: 103 tests and 3 subtests.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/llm-wiki-ontology-ingest` -> pass: skill is valid.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/llm-wiki-ontology-ingest tests/test_llm_wiki_pipeline_contract.py` -> pass: all checks passed. (`python3 -m ruff` was unavailable in the active interpreter.)
  - `git diff --check` -> pass: no whitespace errors.
- Learnings for future iterations: the repository-local and global ingest skill copies already differed before this story; global synchronization remains intentionally deferred to US-003.
---
## 2026-08-25T02:16:41+09:00 - US-002
- Bound single-source ingest to the existing `wiki_workflow.py` run and fresh final-review fingerprint, and large-source ingest to the existing batch manifest, single writer, strict pipeline check, representative questions, and corpus certification.
- Added a three-attempt same-run repair bound driven by structured blockers and explicit `ready`, `partial`, `not_ready`, and `blocked` completion postures.
- Reused `state/wiki_runs/` and `state/wiki_batches/`; no duplicate workflow ledger or new runtime was added.
- Files changed: `.agents/skills/llm-wiki-ontology-ingest/SKILL.md`, `tests/test_llm_wiki_pipeline_contract.py`, `.codex/ralph/prd.json`, `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest tests/test_llm_wiki_pipeline_contract.py -q` -> pass: 16 tests and 3 subtests.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/llm-wiki-ontology-ingest` -> pass: skill is valid.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check tests/test_llm_wiki_pipeline_contract.py` -> pass: all checks passed.
  - `git diff --check` -> pass: no whitespace errors.
- Learnings for future iterations: completion certification belongs to repo-owned state and fingerprints; the reusable skill should only select and drive those surfaces.
---
## 2026-08-25T02:17:37+09:00 - US-003
- Added proposal-only learning: isolated failures receive local repair, while reusable changes require the same stable failure and component across at least three independent run IDs with one procedure contract digest.
- Limited proposals to bounded operational metadata and expected tests, excluded raw/private content, and required human review before changing skills, contracts, policies, validators, or runtime.
- Explicitly excluded automatic canaries, rollback machinery, daemons, runtime self-modification, gate relaxation, and automatic truth promotion.
- Synchronized the repository-local and global `llm-wiki-ontology-ingest` skill copies and removed the stale Ralph runner branch marker/handoff generated by its false-positive stop.
- Files changed: `.agents/skills/llm-wiki-ontology-ingest/SKILL.md`, `tests/test_llm_wiki_pipeline_contract.py`, `.codex/ralph/prd.json`, `.codex/ralph/progress.txt`, `.codex/ralph/.last-branch` (removed).
- Quality checks run and result:
  - `python3 -m pytest tests/test_llm_wiki_pipeline_contract.py -q` -> pass: 17 tests and 3 subtests.
  - `python3 -m pytest -q` -> pass: 105 tests and 3 subtests.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/llm-wiki-ontology-ingest` -> pass: skill is valid.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/llm-wiki-ontology-ingest tests/test_llm_wiki_pipeline_contract.py` -> pass: all checks passed.
  - `cmp -s .agents/skills/llm-wiki-ontology-ingest/SKILL.md /Users/hoyasung007hotmail.com/.codex/skills/llm-wiki-ontology-ingest/SKILL.md` -> pass: local and global copies are identical.
  - global `quick_validate.py` -> pass: synchronized skill is valid.
  - `git diff --check` -> pass: no whitespace errors.
- Learnings for future iterations: repeated-failure learning should produce review evidence, not grant the ingest operator authority to modify its own gates.
---
## Adaptive Repo Docs lifecycle (started 2026-08-25)

### Codebase Patterns
- Keep one unified Repo Docs profile; optional surfaces activate only when their promotion threshold is met.
- Preserve existing file locations and manifest keys. New defaults must not force migration of mature repositories.
- Treat code/tests and current docs as authority, wiki and SQLite as derived aids, and search output as candidates rather than truth.
- Keep DuckCrab domain vocabulary out of the reusable skill; absorb only general document lifecycle patterns.

## 2026-08-25 - US-001
- Established descriptive relative Markdown links as the Repo Docs wiki default while keeping legacy Obsidian wikilinks compatible.
- Added bounded two-hop/twelve-page traversal guidance and canonical-target verification.
- Added robust local Markdown link validation for missing/outside targets, URLs, anchors, inline/fenced/indented code, footnotes, reference links, nested lists, nested labels, and escaped/balanced paths.
- Updated templates and eval fixture without emitting links to absent optional repo-map or intelligence targets.
- Files changed: repo-docs `SKILL.md`, AGENTS/wiki templates, eval fixture/expectation, validator, and validator tests.
- Quality checks:
  - `python3 -m pytest -q tests/test_repo_docs_intelligence_validator.py` -> 23 passed.
  - `python3 -m pytest -q` -> 171 passed and 26 subtests passed.
  - Ruff -> all checks passed.
  - Skill quick validation -> valid.
  - Independent review -> CLEAN, no Critical/Important/Minor findings.
- Learnings: Markdown validation must understand optional targets and enough CommonMark container context to avoid turning examples into live links or hiding nested-list links.
---
## 2026-08-25T15:19:29+09:00 - US-002
- Added one adaptive authority chain from live code/tests through current docs and accepted ADRs, intelligence contracts, plans/evidence/reviews, derived wiki memory, and optional search indexes.
- Added risk-based promotion rules that keep small changes lightweight and activate ADR, plan, evidence, review, wiki-decision, or repo-map surfaces only when warranted.
- Added minimal ADR, implementation-plan, evidence, review, and wiki-decision templates; decision governance and implementation progress remain separate, and wiki decisions are explicitly non-canonical.
- Preserved flat and custom ADR locations and existing manifest keys; `docs/adr/` is only the default for repositories without an established ADR location.
- Files changed: repo-docs `SKILL.md`, AGENTS/docs-portal templates, five lifecycle templates, focused contract test, `.codex/ralph/prd.json`, and `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest -q tests/test_repo_docs_intelligence_validator.py` -> pass: 24 tests.
  - `python3 -m pytest -q` -> pass: 172 tests and 26 subtests.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/repo-docs-intelligence-bootstrap tests/test_repo_docs_intelligence_validator.py` -> pass: all checks passed.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/repo-docs-intelligence-bootstrap` -> pass: skill is valid.
  - `git diff --check` -> pass: no whitespace errors.
  - Independent read-only re-review -> pass: Critical 0, Important 0, Minor 0.
- Learnings for future iterations: optional lifecycle surfaces must be conditional in both workflow prose and generated portals; contract tests should reject conflicting unconditional paths, not only assert that preferred guidance exists.
---
## 2026-08-25T15:42:36+09:00 - US-003
- Added optional-surface lifecycle validation for conventional, flat, and metadata-declared ADRs, plans, evidence, reviews, and wiki decisions without requiring those directories in small repositories.
- Enforced unique lifecycle identities, decision/implementation/plan statuses, resolvable supersession/plan/evidence/canonical references, implementation plus verification backing for implemented ADRs, non-canonical wiki decisions, canonical-doc and activated-index portal exposure, and non-stale single current next actions.
- Kept dependency-light validation compatible with YAML comments, lists, and legacy wikilinks; aligned startup/change synchronization guidance and portal templates with portable Markdown links.
- Added focused minimal, mature, transitional, malformed-activation, flat-layout, portal-drift, false-completion, stale-plan, and dependency-light regression coverage; refreshed existing eval fixture portals.
- Files changed: repo-docs `SKILL.md`, AGENTS/docs portal/ADR templates, validator, three eval fixture portals, focused validator tests, `.codex/ralph/prd.json`, and `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest -q tests/test_repo_docs_intelligence_validator.py` -> pass: 32 tests.
  - `python3 -m pytest -q` -> pass: 180 tests and 26 subtests.
  - `python3 -S tests/test_repo_docs_intelligence_validator.py -q` -> pass: 32 dependency-light tests.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/repo-docs-intelligence-bootstrap tests/test_repo_docs_intelligence_validator.py` -> pass: all checks passed.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/repo-docs-intelligence-bootstrap` -> pass: skill is valid.
  - `python3 -m py_compile .agents/skills/repo-docs-intelligence-bootstrap/scripts/validate_repo_docs_intelligence.py tests/test_repo_docs_intelligence_validator.py` -> pass.
  - `git diff --check` -> pass: no whitespace errors.
  - Independent read-only review after two repair rounds -> pass: Critical 0, Important 0, Minor 0; ready to merge.
- Learnings for future iterations: optional validation must activate from both recognized locations and metadata, while index files and ambiguous names remain excluded; plans never count as proof of implementation.
---
## 2026-08-25T15:58:41+09:00 - US-004
- Added an opt-in, standard-library-only Repo Docs retrieval CLI with atomic SQLite rebuild, status, FTS5 search, bounded two-hop/twelve-page Markdown-link traversal, and deep doctor checks.
- Indexed only root `AGENTS.md`, `docs/**/*.md`, and `wiki/**/*.md` as heading chunks with document/chunk/corpus fingerprints; source-code bodies, embeddings, rank fusion, ANN, canonical JSONL, workflow engines, and runtime databases remain outside the profile.
- Kept missing retrieval state valid and retrieval failures non-blocking for canonical docs and validator completion; updated the skill, AGENTS template, and docs portal template with measured opt-in guidance and CodeGraph/LSP/`rg` ownership for code navigation.
- Files changed: repo-docs `SKILL.md`, AGENTS/docs portal templates, bundled retrieval CLI, focused retrieval tests, `wiki/_meta/log.md`, `.codex/ralph/prd.json`, and `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest -q tests/test_repo_docs_retrieval.py tests/test_repo_docs_intelligence_validator.py` -> pass: 40 tests.
  - `python3 -m pytest -q` -> pass: 188 tests and 26 subtests.
  - `python3 -S tests/test_repo_docs_retrieval.py -q` -> pass: 8 dependency-light tests.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/repo-docs-intelligence-bootstrap tests/test_repo_docs_retrieval.py tests/test_repo_docs_intelligence_validator.py` -> pass: all checks passed.
  - `python3 /Users/hoyasung007hotmail.com/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/repo-docs-intelligence-bootstrap` -> pass: skill is valid.
  - `python3 -m py_compile .agents/skills/repo-docs-intelligence-bootstrap/scripts/repo_docs_retrieval.py tests/test_repo_docs_retrieval.py` -> pass.
  - `git diff --check` -> pass: no whitespace errors.
  - Generated-profile smoke is included in the focused suite and passed under `python3 -S`.
  - Independent read-only review after the final doctor/test repair -> pass: Critical 0, Important 0, Minor 0; CLEAN.
- Learnings for future iterations: derived document search can remain dependency-light and disposable when status is cheap, doctor recomputes exact source-derived rows, and code navigation stays with code-aware tools.
---
## 2026-08-25T16:27:12+09:00 - US-005
- Certified mature and minimal compatibility with explicit `repo-docs-v1` lifecycle promotion, compatibility-safe legacy inventory validation, actual Markdown-link checking, and a repository-wide content-bound read-only dogfood runner.
- DuckCrab read-only dogfood validated 7 canonical docs, 7 flat ADRs, 17 plans, 69 evidence docs, 25 reviews, 5 repo-map docs, and 34 wiki decisions; no required surface or compatibility issue was missing, and all repository content fingerprints remained unchanged.
- Kept 34 real `wiki.broken_markdown_link` findings as an explicitly allowed target-drift caution; any unlisted validator error still fails dogfood. Recorded the 3 repo-map entrypoint warnings and limited changed-file warning without modifying DuckCrab.
- Added the final impact report distinguishing absorbed patterns, excluded domain-specific behavior, checked-but-unchanged surfaces, remaining cautions, and exact checks. Synchronized the repository skill to the installed global skill at digest `d1586d84c765b899468b238d390f73b2b897b77078a7934d9167cf2073356683`.
- Files changed: repo-docs `SKILL.md`, five lifecycle templates, validator, read-only dogfood runner, focused validator/dogfood tests, final impact report, `.codex/ralph/prd.json`, and `.codex/ralph/progress.txt`.
- Quality checks run and result:
  - `python3 -m pytest -q tests/test_repo_docs_dogfood.py tests/test_repo_docs_intelligence_validator.py tests/test_repo_docs_retrieval.py` -> pass: 46 tests.
  - `python3 -m pytest -q` -> pass: 194 tests and 26 subtests.
  - `python3 -S tests/test_repo_docs_intelligence_validator.py -q && python3 -S tests/test_repo_docs_dogfood.py -q && python3 -S tests/test_repo_docs_retrieval.py -q` -> pass: 34 validator, 4 dogfood, and 8 retrieval tests.
  - `/Users/hoyasung007hotmail.com/.local/bin/ruff check .agents/skills/repo-docs-intelligence-bootstrap tests/test_repo_docs_intelligence_validator.py tests/test_repo_docs_dogfood.py tests/test_repo_docs_retrieval.py` -> pass: all checks passed.
  - local and installed-global `quick_validate.py` -> pass: both skill copies are valid.
  - `python3 -m py_compile ...` -> pass for validator, dogfood runner, and focused tests.
  - mature eval fixture validation with the normal interpreter -> pass: errors 0, advisory changed-files warning 1. An earlier `python -S` attempt against the YAML-bearing fixture failed explicitly because PyYAML is intentionally unavailable; YAML-free dependency-light suites passed separately.
  - DuckCrab content-bound read-only dogfood with all seven required surfaces -> pass with cautions: surface issues 0, `read_only=true`, only the explicitly allowed 34 broken-link errors, plus 4 reported warnings.
  - `git diff --check` and staged diff check -> pass: no whitespace errors.
  - Independent final read-only review after three rounds -> pass: Critical 0, Important 0; ready to merge.
- Learnings for future iterations: compatibility certification must separate strict new-contract validation, legacy structural checks, and explicitly accepted target drift; read-only claims need content-bound whole-repository evidence rather than documentation-only fingerprints.
---
