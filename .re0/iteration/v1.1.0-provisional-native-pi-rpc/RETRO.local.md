# Re0 memo and next-cycle gate

Keep the native session boundary and existing UI. Discard the assumption that preserving browser message text preserves an agent session. Native tool results and compaction belong to Pi; Studio owns a view and an identity mapping. The three-turn read/recall/write scenario in EVIDENCE.local.md demonstrates the boundary directly.

Anti-pattern: completion collapses ownership. A completed answer can still have an outstanding control request, and a canceled startup can still own a live child. Gate: delay postprompt state response, deliver settled, then attempt a next turn; no later turn may be killed by old cleanup. Cancel constructor startup and force termination failure; both live reference and persisted PID plus writer lease must survive. Preserve these negative tests before simplifying lifecycle code.

Anti-pattern: verified-looking display substitutes for evidence. Generic read/bash/write activity does not establish citation validity or FTS/vector/link usage. Gate: native outputs carry no inherited verified references; unknown metrics remain unknown and native file writes cannot certify the wiki. Preserve existing gate modules unchanged.

Anti-pattern: local compatibility becomes platform parity. The initial untrusted extension did not load, and a matching model name does not establish matching resources. Gate: for Windows compare installed version, executable, cwd, model, thinking, trust/resources and the same staged read/followup/write/abort/close cases in terminal and RPC. Explicitly report custom TUI differences. Default cutover waits for this proof.

Vocabulary: native session = Pi transcript plus runtime context; turn = one accepted request through settled; dispatch barrier = unfinished control reconciliation; live ownership = process reference plus registry PID plus held lease; browser history = display projection. These contracts, the actual UI and the negative corpus earn reuse. No new agent framework or repository restart is justified by this cycle.

Next cycle: actual Windows parity on the user's machine, followed by a decision on default mode only after the result. A new transcript import UI, arbitrary TUI rendering and inferred citation extraction are separate scope, not prerequisites to keeping the opt-in slice.

Self-review: shower found missing launch prerequisites, ambiguity about read-only enforcement and browser-history loss; usage now states those limits. Read-only SSOT audit identified old blanket chat claims in README, architecture and data flow; current docs distinguish default and native paths and link the operation guide. No separate SSOT consolidation was performed. Re0 removed the stale migration impact inventory. Detool skipped: this is a Pi-specific runbook, not a portability claim. External factchk skipped: outcomes here are local executable observations, not external-world assertions. Mandela limits are recorded in EVIDENCE.local.md.
