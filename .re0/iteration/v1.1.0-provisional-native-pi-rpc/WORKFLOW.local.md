# This cycle

1. FRAME: inspect current Pi adapter and installed RPC contract; use the completed Architect/Critic plan review and recheck the bounded implementation choices before edits.
2. BUILD: add a native-session adapter in runtime, opt-in launch capability, root/conversation/turn routing and conservative writer ownership; no default change.
3. BUILD: connect the existing chat surface to generic native events, native-session labels/statistics, interaction requests and explicit stop/close. Preserve default wiki behavior and browser history compatibility.
4. DRIVE: run focused lifecycle failures and a real native three-turn disposable-vault conversation through HTTP and the actual browser. Exercise follow-up, reload/reconnect, abort and native interaction responses; record differences rather than hide them.
5. REVIEW: separate read-only reviewer checks source/ownership/evidence. Repair important findings and rerun affected proof.
6. RE0-MEMO: extend this casebook with lessons, negative cases and the next gate. Re0 current docs from final behavior rather than append contradictory claims.
7. DECIDE: keep/iterate the proven native slice in place. Windows comparison and default cutover stay unpassed until external evidence arrives. Do not start a wholesale re0-work restart.
